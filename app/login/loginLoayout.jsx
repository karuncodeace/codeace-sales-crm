export default function LoginLayout({ children }) {
    return (
      <html lang="en">
        <body className="bg-red-100 min-h-screen flex items-center justify-center">
          {children}
        </body>
      </html>
    );
  }
  